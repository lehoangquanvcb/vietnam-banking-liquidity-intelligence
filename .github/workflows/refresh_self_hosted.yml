name: Refresh Banking Liquidity Data
on:
  workflow_dispatch:
  schedule:
    - cron: "15 1 * * 1-5"
permissions:
  contents: write
jobs:
  refresh:
    runs-on: [self-hosted]
    steps:
      - uses: actions/checkout@v4
      - name: Refresh data and models
        shell: powershell
        run: |
          $py="$env:USERPROFILE\.venv\Scripts\python.exe"
          if (-not (Test-Path $py)) { throw "Bronze Python not found: $py" }
          & $py scripts/export_manual_interbank.py
          & $py scripts/refresh_data.py
          & $py scripts/build_models.py
      - name: Commit outputs
        shell: powershell
        run: |
          git config user.name "vnstock-liquidity-bot"
          git config user.email "vnstock-liquidity-bot@users.noreply.github.com"
          git add data
          git diff --cached --quiet
          if ($LASTEXITCODE -eq 0) { exit 0 }
          git commit -m "Automated banking liquidity refresh"
          git push
